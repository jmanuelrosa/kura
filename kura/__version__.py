"""Release version for `kura --version`.

Bump this to match the tag (without the leading `v`) before pushing a release.
Development checkouts may carry a `-dev` suffix between tags.
"""

__version__ = "0.6.1"
